# Bundled installers (large, frequently re-downloaded)
deps/
# Compiled installer output
output/
# IDE
*.iss.bak

# Auto-incrementing build counter (each developer's machine has its own)
.build_number
